---
description: Quét dự án và thiết lập Database Mạng nhện của VibeCode.
---

1. Sử dụng công cụ `run_command` để thực thi dòng lệnh sau trên môi trường của tôi để quét dự án:
// turbo
```bash
.\vibecode.bat scan
```
2. Sau khi có kết quả, hãy tóm tắt ngắn gọn báo cáo số file đã được quét và thông báo cho người dùng rằng CSDL Graph (VibeCode) đã sẵn sàng để vẽ sơ đồ hoặc sinh ngữ cảnh.
